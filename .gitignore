# ── Secrets / keys — never commit ──
extension-key.pem
*.pem
client_secret_*.json
.env
.env.local
.env*.local

# ── Dependencies / build output ──
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# ── OS / editor ──
.DS_Store
